# Shared Infrastructure Environment Variables
# Managed by 1Password - Update with: op item get "Homelab-{service}" --field="password" --vault="Homelab" --reveal

# PostgreSQL Configuration
POSTGRES_ADMIN_USER=postgres
POSTGRES_ADMIN_PASSWORD=op://Homelab/Homelab-Postgres-Admin/password

# Redis Configuration  
REDIS_PASSWORD=op://Homelab/Homelab-Redis/password